# Lab 1 screenshots

Add image files here for your report. Suggested names (arbitrary but easy to grade):

| Example file | Content |
|----------------|--------|
| `01-agentgateway-ui.png` | UI / routes / backends (if applicable) |
| `02-curl-llm-response.png` | Successful LLM response via gateway (JSON may be cropped) |
| `03-kagent-agents.png` | kagent agent list |
| `04-kagent-chat-tools.png` | Chat with a tool call (e.g. `k8s_get_resources`) |

Do **not** include API keys, full tokens, or secrets in screenshots.

An empty directory is fine until you add files; `lab-01/README.md` already points to this folder.
